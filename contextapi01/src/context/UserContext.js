import React from "react";

const UserContext = React.createContext();

export default UserContext;


// UserContext can be said as global variables; that gives access to all the components 
/* <UserContext>
    <Login></Login>
    <Signup />

</UserContext> */

